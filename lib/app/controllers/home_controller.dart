import 'package:flutter/foundation.dart';

// [MVC - CONTROLLER]
// HomeController manages the state and logic for the Home screen.
//
// Currently it is a placeholder — the home screen is simple.
// As you add features (notifications, user profile, tracking items),
// those will go here instead of inside the HomePage widget.

class HomeController extends ChangeNotifier {
  // Future additions go here, for example:
  //   UserModel? currentUser;
  //   List<TrackingItem> items = [];
  //   bool isLoading = false;
  //
  //   Future<void> loadDashboard() async { ... }
}
